import colors from "../../../colors";

const commonFooterThemeProps = {
  backgroundColor: colors.secondary.main,
  color: colors.getContrastText(colors.secondary.main),
};

export default commonFooterThemeProps;
